// predawn Cargo.toml

```
auto-register = ["rudi/auto-register", "predawn-macro/auto-register"]
```

become

```
auto-register = ["predawn-macro/auto-register"]
```

// predawn-sea-orm Cargo.toml

```
predawn = { workspace = true, features = ["auto-register"] }
```

become

```
predawn = { workspace = true }
```


preserve_order order json

remove generic bound when defining compound type



Tag
SecuritySchema

controller: condition, skip

ToSchema / ToParameter
/ MultiRequestMediaType / MultiResponseMediaType
/ SingleResponse / MultiResponse :

default,
flatten
description,
example,
deprecated,
actual_type

update references in doc comments by search `https://docs.rs`

service attribute macro
operation id
Extension
TypedHeader
Header
Cookie
Base64
Html
split controller trait
more openapi ui
more ToSchema impl
SSE
FILE
WebSocket
Listener trait
end-to-end test-helper edition 2
startup message
handle .unwrap()
validate

docs, docs, docs